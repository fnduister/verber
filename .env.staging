# Staging Environment Configuration for Verber
# This file contains staging-specific environment variables

# Database Configuration (same as prod for now, but could be separate)
POSTGRES_USER=verber_user
POSTGRES_PASSWORD=verber_password_change_this
POSTGRES_DB=verber_db

# Backend Configuration
JWT_SECRET=staging-jwt-secret-different-from-production
FRONTEND_URL=https://verber.stage.sicole.com

# Frontend Configuration
REACT_APP_API_URL=https://verber.stage.sicole.com/api
REACT_APP_WS_URL=wss://verber.stage.sicole.com
REACT_APP_ENVIRONMENT=staging

# Staging specific settings
REACT_APP_STAGING=true
REACT_APP_VERSION=staging